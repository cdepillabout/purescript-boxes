{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "spago build",
    "test": "spago --config test.dhall test"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "purescript-psa": "^0.8.2"
  }
}
