{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "spago build",
    "test": "spago --config test.dhall test"
  },
  "devDependencies": {
    "purescript": "^0.14.5",
    "rimraf": "^3.0.2",
    "purescript-psa": "^0.8.2",
    "spago": "^0.20.4"
  }
}
