{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^14.0.0",
    "purescript": "^0.13.6",
    "rimraf": "^3.0.2",
    "purescript-psa": "^0.7.3"
  }
}
